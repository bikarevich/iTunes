module.exports = function () {
    var MediaPlayerBuilder = function(options) {
        var that = this,
            $wrap = options.element;

        console.log(options);


        init();

        function init() {

        }

    };
    return (MediaPlayerBuilder);
};